{
  "name": "plinth",
  "version": "0.0.0",
  "description": "Demo of Plinth: browser-based modular audio processing",
  "main": "entry.js",
  "private": true,
  "scripts": {
    "build": "mkdir -p build && cp -r public/ build && webpack --progress --colors --optimize-minimize",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "webpack --progress --colors --watch"
  },
  "author": "Russel Simmons <russ@rsimmons.org>",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.21.0",
    "babel-loader": "^6.2.10",
    "babel-preset-es2015": "^6.18.0",
    "raw-loader": "^0.5.1",
    "webpack": "^1.14.0"
  },
  "dependencies": {
    "drag-drop-webkit-mobile": "^1.2.0",
    "file-saver": "^1.3.3"
  }
}
